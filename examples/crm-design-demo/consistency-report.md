---
doc_version: 2
updated: 2026-06-19
status: reviewed
---

# CRM 设计集 — 一致性审核报告（Phase 5）

> 按 skill Rubric C 跨全套(01–14 + 原型)核对。串行门禁下各篇已 optimized,本报告做整体对齐。
> **v2(Set v1.1):** 原 2 个 Major 开口项(后台页、i18n/主题)已在 `prototype-vue` 闭环补完并实测——见文末"缺口处置"。

## 核对结果
| 维度 | 结果 | 说明 |
|------|:--:|------|
| 术语一致 | ✅ | lead/opportunity/follow_up/quote/SOP/话术/客户全景 全集统一 |
| 实体&属性对齐 | ✅ | [05 数据] 实体 ↔ [02 领域] ↔ [06 接口] 字段一致;无 API 引用未建模实体 |
| 范围一致 | ✅ | 售后→CSM、订单→订单域、主数据→客户全景,01/02/03 口径一致 |
| 决策一致 | ✅ | 模块化单体 / REST / 四库 / 异步队列 在 03 ADR ↔ 09 选型 ↔ 07 异步 ↔ 05 数据 一致 |
| 功能树覆盖 | ✅ | 操作均挂 角色/FR/接口;原型已覆盖坐席工作台 + 后台 SOP/话术/统计 + **B3 角色、B4 渠道规则**(v1.1 补完);开放 API 无界面由 06 覆盖 |
| 角色一致 | ✅ | R1–R4 在 01 矩阵 ↔ 04 角色列 ↔ 06 endpoint auth 一致 |
| 四库归属 | ✅ | 每实体归一库;跨库仅 id;07 落库(logs/stats)对齐;物理映射=PG 分 schema |
| 异步覆盖 | ✅ | 赢单回写/Hermes/rollup/唤醒/SLA/清理 均在 07,触发可追溯,调度 HA 防重跑 |
| REST 统一 | ✅ | 06 已声明 REST;04/14 端点均资源化;事件归 07 |
| 可追溯(FR→设计→测试) | ✅ | 见下表(抽样);无孤儿 FR |
| 编号&引用 | ✅ | 01–14 无重号;08→10 改名后引用已修(仅 00-PROGRESS 待刷) |

## 缺口与处置
- **缺口①(Major,原型 vs 设计)→ ✅ 已闭环(v1.1):** 补出 `AdminRoles.vue`(角色×权限矩阵)、`AdminRules.vue`(渠道/分配规则)+ 导航 + 路由;编译 `dist/` 实测渲染(roles 28 开关、rules 5 渠道+策略)。开放 API 无界面由 06 覆盖,非缺口。
- **缺口②(Major,原型 vs NFR)→ ✅ 已闭环(v1.1):** `prototype-vue` 实装 **vue-i18n 中英**(`el-config-provider` 动态 locale)+ **EP 暗色 + 亮/暗/随系统**(顶栏切换);编译 `dist/` 实测中↔英、暗↔亮两态。视图正文以中文为主、shell/导航已外化(可扩展全量),记为显式可扩展项。
- **缺口③(Minor)→ ✅ 已修:** `00-PROGRESS.md` 旧名引用已改为 `10-工程目录结构.md`。

## 可追溯抽样(FR → 设计 → 测试)
| FR | 设计 | 测试([12]) |
|----|------|------|
| FR-L2 查重 | 06 `/leads:dedup` · 05 lead.dedup_of · 02 BR-1 | 单元+集成 |
| FR-O3 赢单回写 | 03 流程B · 07 异步任务 · 06 `:won` | 异步集成+E2E |
| FR-O4 折扣权限 | 02 BR-2 · 05 special_price_approval · 06 special-price | 单元+安全 |
| FR-S3 SOP 查询 | 06 `/api/v1/sop` · 04 C3 | 集成 |

## 结论
核心设计**内部自洽**;原两个 Major(后台页面、i18n/主题)已在 `prototype-vue` **闭环补完并实测**,**无遗留开口项**(原型 = 设计)。→ **Set version v1.1(闭环完成)**。
